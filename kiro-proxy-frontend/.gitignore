# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage
/coverage

# Vitest artifacts (browser screenshots/attachments)
**/__screenshots__/
.vitest-attachments/
# 环境变量：只提交 .env.example，真实地址一律留在本机
# Environment files: commit only .env.example, keep real hostnames local
.env.*
!.env.example

# TanStack Router 缓存 / TanStack Router cache
.tanstack/

# 本地打包的源码压缩包 / locally produced source archives
*.zip

# 本机保留的上游模板 git 历史（本项目基于 shadcn-admin）
# Local-only upstream template history (this project derives from shadcn-admin)
.git-upstream-shadcn-admin.local/
# 来自其它项目的遗留 spec（与本仓库代码无关），保留在本机但不公开
# Leftover spec from an unrelated project. Kept locally, not published.
.kiro/specs/matching-verification-console/
